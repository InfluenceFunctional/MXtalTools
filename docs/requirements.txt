# Sphinx documentation build dependencies.
# Pure-Python mxtaltools deps (numpy, scipy, etc.) are installed via
# `pip install .` in .readthedocs.yaml, not listed here.
sphinx>=7.1
sphinx-rtd-theme>=2.0
